"""
The Status (Initialization) view of the PypeIt Dashboard.

Stage 0 provides only an empty, labeled placeholder; the state-first status
table, summary strip, scope drop-downs, and configuration-overview
navigator are built in Stage 2.

Generated by JXP and Claude.
"""

from qtpy.QtWidgets import QWidget, QVBoxLayout, QLabel
from qtpy.QtCore import Qt


class StatusView(QWidget):
    """
    Placeholder widget for the Status tab (Stage 0).

    Generated by JXP and Claude.

    Args:
        parent (:obj:`QWidget`, optional):
            The parent widget.
    """

    def __init__(self, parent=None):
        super().__init__(parent=parent)
        layout = QVBoxLayout(self)
        # A clearly labeled stub so the empty tab reads as intentional.
        placeholder = QLabel('Status view — coming in Stage 2')
        placeholder.setAlignment(Qt.AlignCenter)
        placeholder.setStyleSheet('color: grey; font-style: italic;')
        layout.addWidget(placeholder)
